{
  "build": {
    "command": "npm install @rollup/rollup-linux-x64-gnu --save-optional && npm run build",
    "directory": "dist",
    "environment": {
      "NODE_VERSION": "20",
      "NPM_CONFIG_INCLUDE": "optional"
    }
  }
} 