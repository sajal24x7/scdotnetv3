# 05 — Dead code & repo cleanup (P2)

Everything below was verified unreferenced on 2026-07-07 by grepping `src/`, `public/`,
`scripts/`, and `astro.config.mjs`. **Re-verify before each deletion** — the check is one
grep:

```bash
grep -rn "<NameOrFilename>" src public scripts astro.config.mjs package.json | grep -v "<the file itself>"
```

If the grep returns any live reference, skip that deletion and note it in the PR.

## 5.1 Delete unused components

| File | Evidence |
|------|----------|
| `src/components/SearchButton.astro` | imported nowhere |
| `src/components/Search.astro` | legacy pre-Pagefind search; imported nowhere (contains comment "Force rebuild - search page removed"); embeds ALL posts into HTML via `define:vars` — must not come back |
| `src/components/content/Search.astro` | second legacy search input, imported nowhere |
| `src/components/content/FeaturedPosts.astro` | imported nowhere |
| `src/components/content/HomeFeaturedGrid.astro` | dead since the unified-feed homepage redesign (PR #327) |
| `src/components/content/ResponsiveContentGrid.astro` | imported nowhere |
| `src/components/content/FictionGrid.astro` | imported nowhere |
| `src/components/content/BookList.astro` | imported nowhere |
| `src/components/content/Pagination.astro` | imported nowhere |
| `src/components/content/PostList.astro` | imported nowhere (distinct from `src/components/PostList.astro`) |
| `src/components/PostList.astro` + `src/components/PostCard.astro` | `PostList` imported nowhere; `PostCard` only imported by `PostList` — delete both together |
| `src/components/layout/StreamList.astro` | imported nowhere |
| `src/components/layout/FeaturedGrid.astro` | imported nowhere (grep hit in `GardenGrid.astro` is a comment/string — confirm before deleting) |
| `src/components/ui/BookShowcase.astro` | imported nowhere |
| `src/components/ui/SectionHeader.astro` | imported nowhere |

## 5.2 Delete unused data/endpoints/scripts

| File | Evidence |
|------|----------|
| `src/pages/search-index.json.ts` | endpoint fetched by nothing; `/search/` uses Pagefind (`/pagefind/pagefind.js`) |
| `src/data/homeFeatured.json` | referenced nowhere since homepage redesign |
| `src/data/featuredPostSlugs.ts` | referenced nowhere |
| `public/logo-switch.js` | not loaded by any page (Layout has its own inline theme script) |
| `src/pages/navigation-demo.astro` | internal test page, publicly deployed and in the sitemap |
| `check_2025_categories.js` (repo root) | one-off migration checker |
| `package-lock 2.json` (repo root) | accidental duplicate of `package-lock.json` (334 KB) |

## 5.3 Stale configuration notes

- **`.npmrc`**: the `legacy-peer-deps=true` entry is justified by a comment about
  `@astrojs/tailwind@6`, which is **no longer in `package.json`** (the project moved to
  `@tailwindcss/vite`). Test removal:

  ```bash
  # remove the legacy-peer-deps line and its comment, then:
  rm -rf node_modules package-lock.json.bak && npm install --dry-run
  ```

  If `npm install` and `npm ci` succeed without peer-dep errors, keep the removal;
  otherwise restore and update the comment to name the actual offending package.

- **`cloudflare-pages.json` / workflows**: Node version mismatch handled in brief 07 —
  don't touch here.

## 5.4 Update stale documentation

These docs describe deleted features and will mislead future automation:

- `AGENTS.md` — "Card Components" section: the **Homepage Featured / `homeFeatured.json`**
  paragraph (last bullet) must be removed once 5.2 lands; the claim that `PostCard` "is
  consumed by `PostList` when a route wants stacked post cards" becomes false after 5.1.
  Rewrite the section to describe the current reality: homepage renders
  `UnifiedFeed`; cards in use are `Card` (via `GardenGrid`), `bookshelf/BookCard`,
  shelf cards (`FilmCard`/`TVCard`/`GameCard`), and `PostItem` for stream entries.
- `README.md` — no changes strictly required, but confirm the "Progress pages" bullet
  (`/now/` and `/done/`) matches reality: the second page is `/then/`
  (`src/pages/then.astro`), not `/done/`. Fix the path name.
- `docs/components/search.md` — check it documents the Pagefind implementation, not the
  deleted legacy components; update if it references `Search.astro` or
  `search-index.json`.

## 5.5 Repo housekeeping (no code impact)

- `docs/` and `planning/` both contain historical audits; leave them, but this folder
  (`planning/site-audit-2026-07/`) supersedes `planning/site-audit-followup-task-list.md`
  where they overlap.
- `.gitignore`: confirm `dist/`, `node_modules/`, `.astro/` are present (they are), and
  add `package-lock 2.json`-style artifacts are not regenerated by any tooling (it isn't —
  it was a manual copy accident; safe to delete).

## Order of operations

1. Run the verification grep for every file in 5.1/5.2 (they're cheap).
2. Delete in one commit; `npm run build` must still pass.
3. `.npmrc` change in its own commit (so it can be reverted independently).
4. Docs updates last, describing the post-deletion state.

## Verification

```bash
npm install          # fresh, after .npmrc change
npx astro build      # or npm run build (full pipeline)
grep -rn "HomeFeaturedGrid\|homeFeatured\|SearchButton\|search-index.json" src public && echo "LEFTOVER REFS" || echo OK
test -f "package-lock 2.json" && echo "still there" || echo OK
```

Also click through: homepage, /garden/, /stream/, /bookshelf/, one post, /search/ — all
render identically to before (this brief must be behavior-neutral).
