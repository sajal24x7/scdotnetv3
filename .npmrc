# Explicitly include optional dependencies
include=optional
# Force npm to install optional dependencies
optional=true
# Use exact versions from lockfile
package-lock-only=false
# Disable audit during CI builds for performance
audit=false
# Include platform-specific dependencies
target_platform=linux
target_arch=x64 