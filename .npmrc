# Disable audit during CI builds for performance
audit=false
# @astrojs/tailwind@6 declares peer compat with astro ^3||^4||^5 only;
# it works at runtime with Astro 6 but strict npm ci fails without this.
# Remove once Tailwind v4 migration replaces @astrojs/tailwind entirely.
legacy-peer-deps=true