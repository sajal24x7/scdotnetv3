---
import Layout from '../../../layouts/Layout.astro';
import ContentGrid from '../../../components/ContentGrid.astro';
import PageHeader from '../../../components/layout/PageHeader.astro';
import { getAllPosts, transformPost } from '../../../utils/content';

// Get all micro posts
const allPosts = await getAllPosts();
const microPosts = allPosts
  .filter(post => post.data.category === 'micro')
  .sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime());

// Transform posts for the ContentGrid component
const transformedPosts = microPosts.map(post => ({
  data: {
    title: post.data.title,
    description: post.data.description,
    pubDate: post.data.pubDate,
    category: post.data.category,
    image: post.data.image,
    link: `/${post.data.category}/${post.slug}/`
  },
  body: post.body,
  render: post.render
}));
---

<Layout 
  title="Micro - Stream" 
  description="Micro posts and quick updates" 
  currentPage="/stream/micro/"
  pageWrapper={{
    maxWidth: '7xl',
    padding: 'none',
    centered: true,
    className: 'px-2'
  }}
>
    <PageHeader 
      title="Micro"
      description="Short micro posts and quick updates from daily life."
      layout="centered"
      size="lg"
    />
    
    <div class="micro-count mb-6 text-center">
      {transformedPosts.length} {transformedPosts.length === 1 ? 'micro post' : 'micro posts'}
    </div>
    
    <div class="text-center mb-6">
      <a 
        href="/tags/?category=micro" 
        class="text-blue-600 dark:text-blue-400 hover:text-blue-700 dark:hover:text-blue-300 transition-colors duration-200"
      >
        View entries by tag
      </a>
    </div>

    <main class="micro-main">
      <ContentGrid posts={transformedPosts} />
    </main>
</Layout>

<style>
  .micro-count {
    font-size: 0.875rem;
    font-weight: 500;
    color: #8b5cf6;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  :global(.dark) .micro-count {
    color: #a78bfa;
  }

  .micro-main {
    padding-bottom: 2rem;
  }

  /* Mobile adjustments */
  @media (max-width: 768px) {
    .micro-main {
      padding-bottom: 1.5rem;
    }
  }
</style>