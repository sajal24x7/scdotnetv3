---
import { getCollection } from 'astro:content';
import Layout from '../../../layouts/Layout.astro';
import ContentGrid from '../../../components/ContentGrid.astro';

export async function getStaticPaths() {
  const years = ['2012', '2013', '2014', '2015', '2016', '2017', '2019', '2021', '2022', '2024', '2025'];
  const allPosts = await Promise.all(years.map(year => getCollection(year)));
  const bookshelfPosts = allPosts
    .flat()
    .filter(post => post.data.category === 'bookshelf');

  // Get all unique tags
  const tags = new Set();
  bookshelfPosts.forEach(post => {
    if (post.data.tags) {
      post.data.tags.forEach(tag => tags.add(tag));
    }
  });

  return Array.from(tags).map(tag => ({
    params: { tag },
    props: { tag }
  }));
}

const { tag } = Astro.props;

// Get all posts from year collections
const years = ['2012', '2013', '2014', '2015', '2016', '2017', '2019', '2021', '2022', '2024', '2025'];
const allPosts = await Promise.all(years.map(year => getCollection(year)));
const taggedPosts = allPosts
  .flat()
  .filter(post => 
    post.data.category === 'bookshelf' && 
    post.data.tags && 
    post.data.tags.includes(tag)
  )
  .sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime());

// Transform posts for the ContentGrid component
const transformedPosts = taggedPosts.map(post => ({
  data: {
    title: post.data.title,
    description: post.data.description,
    pubDate: post.data.pubDate,
    category: post.data.category,
    image: post.data.image,
    link: `/${post.data.category}/${post.slug}/`
  },
  body: post.body
}));
---

<Layout 
  title={`Bookshelf entries tagged with "${tag}"`}
  description={`Browse book reviews tagged with "${tag}"`}
  currentPage="/bookshelf/"
>
  <div class="max-w-7xl mx-auto">
    <header class="mb-12 px-6">
      <div class="flex flex-col">
        <h1 class="text-4xl font-bold mb-4">Bookshelf entries tagged with "{tag}"</h1>
        <a 
          href="/bookshelf/tags" 
          class="text-blue-600 dark:text-blue-400 hover:text-blue-700 dark:hover:text-blue-300 transition-colors duration-200"
        >
          ← Back to all tags
        </a>
      </div>
    </header>

    <div class="px-6">
      <ContentGrid posts={transformedPosts} />
    </div>
  </div>
</Layout> 