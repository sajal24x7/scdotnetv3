---
import Card from './Card.astro';

interface Props {
  title: string;
  description: string;
  url: string;
  created: Date;
  image?: string;
}

const { title, description, url, created, image } = Astro.props;
---

<Card 
  title={title}
  description={description}
  created={created}
  category="blog"
  image={image}
  link={url}
/> 