---
interface Props {
  tags: string[];
  title?: string;
}

const { tags, title = "Tags" } = Astro.props;
---

<aside class="tag-sidebar">
  <div class="tag-sidebar-content">
    <h3 class="tag-sidebar-title">{title}</h3>
    <div class="tag-list">
      {tags.map((tag) => (
        <a href={`/tags/${tag.toLowerCase()}/`} class="tag-item">
          #{tag}
        </a>
      ))}
    </div>
  </div>
</aside>

<style>
  .tag-sidebar {
    background: #f9fafb;
    border: 1px solid #e5e7eb;
    border-radius: 0.5rem;
    padding: 1.5rem;
    height: fit-content;
  }

  :global(.dark) .tag-sidebar {
    background: #1f2937;
    border-color: #374151;
  }

  .tag-sidebar-content {
    width: 100%;
  }

  .tag-sidebar-title {
    font-size: 1.125rem;
    font-weight: 600;
    margin-bottom: 1rem;
    color: #111827;
    border-bottom: 2px solid #e5e7eb;
    padding-bottom: 0.5rem;
  }

  :global(.dark) .tag-sidebar-title {
    color: #f9fafb;
    border-bottom-color: #374151;
  }

  .tag-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .tag-item {
    display: block;
    color: #6b7280;
    text-decoration: none;
    padding: 0.5rem 0.75rem;
    border-radius: 0.375rem;
    transition: all 0.2s ease;
    border: 1px solid transparent;
    font-size: 0.875rem;
    line-height: 1.4;
  }

  .tag-item:hover {
    color: #8b5cf6;
    background: #f3f4f6;
    border-color: #e5e7eb;
    transform: translateX(0.25rem);
  }

  :global(.dark) .tag-item {
    color: #9ca3af;
  }

  :global(.dark) .tag-item:hover {
    color: #a78bfa;
    background: #374151;
    border-color: #4b5563;
  }

  /* Active state for current tag */
  .tag-item.active {
    color: #8b5cf6;
    background: #ede9fe;
    border-color: #a78bfa;
    font-weight: 500;
  }

  :global(.dark) .tag-item.active {
    color: #a78bfa;
    background: #2d1b69;
    border-color: #6d28d9;
  }

  /* Responsive adjustments */
  @media (max-width: 1023px) {
    .tag-sidebar {
      display: none;
    }
  }

  /* Scrollable on very tall lists */
  @media (min-height: 800px) {
    .tag-list {
      max-height: 60vh;
      overflow-y: auto;
      scrollbar-width: thin;
    }

    .tag-list::-webkit-scrollbar {
      width: 4px;
    }

    .tag-list::-webkit-scrollbar-track {
      background: #f3f4f6;
    }

    .tag-list::-webkit-scrollbar-thumb {
      background: #d1d5db;
      border-radius: 2px;
    }

    :global(.dark) .tag-list::-webkit-scrollbar-track {
      background: #1f2937;
    }

    :global(.dark) .tag-list::-webkit-scrollbar-thumb {
      background: #4b5563;
    }
  }

  /* Compact mode for smaller sidebars */
  .tag-sidebar.compact {
    padding: 1rem;
  }

  .tag-sidebar.compact .tag-sidebar-title {
    font-size: 1rem;
    margin-bottom: 0.75rem;
  }

  .tag-sidebar.compact .tag-item {
    padding: 0.375rem 0.5rem;
    font-size: 0.8rem;
  }
</style>
